# -*- coding: utf-8 -*-
"""Untitled20.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1nrzcW3VK1OEMOeWQp7jri1bI5weA_knt
"""

# 1. Load the Iris dataset and display the first five records

import pandas as pd

# Load dataset
df = pd.read_csv("Iris.csv")

# Display first five records
print("First Five Records:")
print(df.head())

print(df.isnull().sum())

numeric_cols = df.select_dtypes(include='number').columns
df[numeric_cols] = df[numeric_cols].fillna(df[numeric_cols].mean())

print(df.head())

numeric_df = df.select_dtypes(include='number')

correlation_matrix = numeric_df.corr()

print(correlation_matrix)

# 5. Visualize the correlation matrix using a heatmap

numeric_df = df.select_dtypes(include='number')

correlation_matrix = numeric_df.corr()

plt.figure(figsize=(6,5))
sns.heatmap(correlation_matrix, annot=True, cmap="coolwarm", fmt=".2f")
plt.title("Correlation Heatmap of Iris Dataset")
plt.show()

# 6. Identify highly correlated feature pairs

threshold = 0.8

for i in range(len(correlation_matrix.columns)):
    for j in range(i + 1, len(correlation_matrix.columns)):
        if abs(correlation_matrix.iloc[i, j]) > threshold:
            print(correlation_matrix.columns[i], "-", correlation_matrix.columns[j], ":", correlation_matrix.iloc[i, j])

# 7. Data cleaning by detecting and filling missing values

print("Missing values before cleaning:")
print(df.isnull().sum())

numeric_cols = df.select_dtypes(include='number').columns
df[numeric_cols] = df[numeric_cols].fillna(df[numeric_cols].mean())

print("\nCleaned Dataset:")
print(df)

# 8. Perform data cleaning and data reduction

# Data Cleaning
numeric_cols = df.select_dtypes(include='number').columns
df[numeric_cols] = df[numeric_cols].fillna(df[numeric_cols].mean())

# Data Reduction (Correlation Analysis)
correlation_matrix = df[numeric_cols].corr()

print("Cleaned Dataset:")
print(df)

print("\nCorrelation Matrix:")
print(correlation_matrix)

# 9. Complete preprocessing of the Iris dataset

# Fill missing values
numeric_cols = df.select_dtypes(include='number').columns
df[numeric_cols] = df[numeric_cols].fillna(df[numeric_cols].mean())

# Correlation matrix
correlation_matrix = df[numeric_cols].corr()

# Heatmap
plt.figure(figsize=(6,5))
sns.heatmap(correlation_matrix, annot=True, cmap="coolwarm", fmt=".2f")
plt.title("Correlation Heatmap")
plt.show()

# Highly correlated features
threshold = 0.8

print("Highly Correlated Features:")
for i in range(len(correlation_matrix.columns)):
    for j in range(i + 1, len(correlation_matrix.columns)):
        if abs(correlation_matrix.iloc[i, j]) > threshold:
            print(correlation_matrix.columns[i], "-", correlation_matrix.columns[j], ":", correlation_matrix.iloc[i, j])